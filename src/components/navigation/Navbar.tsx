"use client"

const Navbar = () => {
  return (
    <div className="fixed top-0 left-0 right-0 h-16 flex items-center justify-center bg-red-600">
        <nav className="w-full max-w-7xl flex items-center justify-between">
            {/* logo */}
            <div>
            </div>
            {/* menus */}
            <div>

            </div>

            <div>
            </div>
        </nav>
    </div>
  );
};

export default Navbar;
